// FETCHES_TARGET is the current number we want to set 'fetches' to when a worker reports back
// It is representative of the macro iteration of the Forager project
export const FETCHES_TARGET = 2;
export const FORAGER_SERVICE =
	'arn:aws:ecs:us-east-1:781426676209:service/Foragers/Foraging-Service';
